language: python
python:
  - 2.6
  - 2.7

install:
  - make python=$( which python ) .installed.cfg

script:
  - make tests

notifications:
  irc:
    - "irc.freenode.org#sprint"
  email:
    - nejc.zupan@gmail.com
